// Minimal test to isolate lucide-react dependency issue
import { useState } from 'react';

export default function App() {
  const [count, setCount] = useState(0);
  
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Test Build</h1>
      <button onClick={() => setCount(count + 1)}>
        Count: {count}
      </button>
      <p>If this builds, the issue is in the imported components.</p>
    </div>
  );
}
