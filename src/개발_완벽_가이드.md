# 🎯 YouTube 채널 분석 서비스 - 완벽 개발 가이드

> 코드 다운로드부터 배포까지, 모든 단계를 빠짐없이 상세하게 설명합니다.  
> 초보자도 따라할 수 있도록 작성되었습니다.

---

## 📋 목차

1. [개발 준비 단계](#1-개발-준비-단계)
2. [프로젝트 초기 설정](#2-프로젝트-초기-설정)
3. [프로젝트 구조 완벽 이해](#3-프로젝트-구조-완벽-이해)
4. [개발 환경 구축](#4-개발-환경-구축)
5. [단계별 개발 순서](#5-단계별-개발-순서)
6. [각 기능별 개발 가이드](#6-각-기능별-개발-가이드)
7. [테스트 및 디버깅](#7-테스트-및-디버깅)
8. [빌드 및 배포 준비](#8-빌드-및-배포-준비)
9. [배포 실행](#9-배포-실행)
10. [배포 후 관리](#10-배포-후-관리)
11. [문제 해결 가이드](#11-문제-해결-가이드)
12. [고급 개발 팁](#12-고급-개발-팁)

---

## 1. 개발 준비 단계

### 1-1. 필수 프로그램 설치

#### ✅ 체크리스트

진행하기 전 다음 프로그램들이 설치되어 있는지 확인하세요:

- [ ] **Node.js 18.0 이상**
  - 확인: `node --version`
  - 다운로드: https://nodejs.org
  - 권장: LTS 버전 (20.x 이상)

- [ ] **npm 9.0 이상** (Node.js와 함께 설치됨)
  - 확인: `npm --version`

- [ ] **Git 2.0 이상**
  - 확인: `git --version`
  - 다운로드: https://git-scm.com

- [ ] **코드 에디터** (선택사항, 하지만 강력 추천)
  - VS Code: https://code.visualstudio.com
  - Cursor: https://cursor.sh (AI 기능 포함)

#### 📦 추가 도구 (선택)

- **Chrome DevTools** - 브라우저 디버깅
- **React DevTools** - React 컴포넌트 디버깅
  - Chrome 확장 프로그램 설치
- **Postman** - API 테스트 (YouTube API 테스트용)

### 1-2. 개발 환경 확인

#### 운영체제 확인

```bash
# macOS
sw_vers

# Windows
ver

# Linux
lsb_release -a
```

#### 필수 도구 버전 확인

```bash
# 모든 필수 도구 버전 한번에 확인
node --version    # v18.0.0 이상
npm --version     # 9.0.0 이상
git --version     # 2.0.0 이상
```

모두 설치되었다면 다음 단계로!

---

## 2. 프로젝트 초기 설정

### 2-1. 코드 다운로드

#### 방법 1: Figma Make에서 직접 다운로드

1. **Figma Make 프로젝트 열기**
   - Figma Make에서 이 프로젝트를 엽니다

2. **다운로드 버튼 클릭**
   - "Download" 또는 "Export" 버튼 찾기
   - ZIP 형식으로 다운로드

3. **압축 해제**
   ```bash
   # 작업 폴더로 이동
   cd ~/Documents/Projects  # macOS/Linux
   cd C:\Users\내이름\Documents\Projects  # Windows

   # ZIP 파일 압축 해제
   unzip youtube-channel-consultant.zip
   
   # 압축 해제된 폴더로 이동
   cd youtube-channel-consultant
   ```

#### 방법 2: Git Clone (GitHub에 있는 경우)

```bash
# 작업 폴더로 이동
cd ~/Documents/Projects  # 원하는 위치로 변경

# Git Clone
git clone https://github.com/사용자명/youtube-channel-consultant.git

# 폴더 이동
cd youtube-channel-consultant
```

### 2-2. 프로젝트 구조 확인

다운로드 후 폴더 구조가 올바른지 확인:

```bash
# macOS/Linux
ls -la

# Windows
dir
```

**확인할 주요 파일:**

```
youtube-channel-consultant/
├── package.json          ← 필수! 이 파일이 없으면 잘못된 폴더
├── src/
│   ├── main.tsx         ← 앱 진입점
│   └── App.tsx          ← 메인 앱 파일
├── components/          ← 페이지 컴포넌트
├── styles/
│   └── globals.css      ← 전역 스타일
├── vite.config.ts       ← Vite 설정 (있을 수도)
└── tsconfig.json        ← TypeScript 설정
```

✅ **`package.json` 파일이 보이면 올바른 위치입니다!**

### 2-3. package.json 파일 확인

`package.json` 파일을 열어서 확인:

```bash
# 파일 내용 확인
cat package.json  # macOS/Linux
type package.json  # Windows
```

**확인할 내용:**

```json
{
  "name": "youtube-channel-consultant",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",           ← 개발 서버 실행
    "build": "vite build",   ← 프로덕션 빌드
    "preview": "vite preview" ← 빌드 미리보기
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    // ... 기타 의존성
  }
}
```

### 2-4. 의존성 패키지 설치

**가장 중요한 단계입니다!**

```bash
# npm을 사용하여 모든 패키지 설치
npm install

# 또는 줄여서
npm i
```

**설치 과정:**

```
설치 시작...
├── react@18.2.0
├── react-dom@18.2.0
├── @radix-ui/react-dialog@1.0.0
├── lucide-react@0.300.0
├── tailwindcss@4.0.0
└── ... (100개 이상의 패키지)

총 설치 시간: 2-5분
총 용량: 200-400MB
```

**설치 완료 확인:**

```bash
# node_modules 폴더 생성 확인
ls -la node_modules  # macOS/Linux
dir node_modules     # Windows

# package-lock.json 생성 확인
ls -la package-lock.json
```

✅ `node_modules` 폴더와 `package-lock.json` 파일이 생성되었으면 성공!

### 2-5. 초기 설정 파일 확인

#### tsconfig.json (TypeScript 설정)

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

#### vite.config.ts (Vite 빌드 도구 설정)

없다면 생성:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    open: true  // 자동으로 브라우저 열기
  }
})
```

#### .gitignore (Git 제외 파일 설정)

```
# 의존성
node_modules/
package-lock.json

# 빌드 결과
dist/
build/

# 환경 변수
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 로그
*.log
npm-debug.log*
```

---

## 3. 프로젝트 구조 완벽 이해

### 3-1. 전체 폴더 구조 맵

```
youtube-channel-consultant/
│
├── 📄 설정 파일들
│   ├── package.json              # 프로젝트 설정 및 의존성
│   ├── tsconfig.json             # TypeScript 설정
│   ├── vite.config.ts            # Vite 빌드 설정
│   ├── vercel.json               # Vercel 배포 설정
│   └── .gitignore                # Git 제외 파일
│
├── 📚 문서들
│   ├── README.md                 # 프로젝트 소개
│   ├── 배포_완벽_가이드.md       # 배포 가이드
│   ├── CURSOR_작업_가이드.md     # AI 개발 가이드
│   └── ... (기타 문서들)
│
├── 🎨 스타일
│   └── styles/
│       └── globals.css           # 전역 CSS (Tailwind 포함)
│
├── 📐 타입 정의
│   └── types/
│       └── index.ts              # TypeScript 인터페이스
│
├── 🔧 유틸리티
│   ├── lib/
│   │   └── storage.ts            # 로컬 스토리지 유틸
│   ├── hooks/
│   │   ├── useApiKey.ts          # API 키 관리 훅
│   │   └── useUserTier.ts        # 사용자 티어 관리 훅
│   └── services/
│       ├── youtube-api.ts        # YouTube API 호출
│       └── ai.ts                 # AI 요약 서비스
│
├── 🧱 UI 컴포넌트
│   └── components/
│       ├── ui/                   # Shadcn UI 컴포넌트 (50개)
│       │   ├── button.tsx
│       │   ├── card.tsx
│       │   └── ...
│       ├── figma/
│       │   └── ImageWithFallback.tsx
│       └── 페이지 컴포넌트들
│           ├── Home.tsx          # 홈 페이지
│           ├── Layout.tsx        # 레이아웃 (헤더, 네비)
│           ├── ApiKeySetup.tsx   # API 키 설정
│           ├── ChannelAnalysis.tsx    # 채널 분석
│           ├── ChannelDetail.tsx      # 채널 상세
│           ├── KeywordAnalysis.tsx    # 키워드 분석
│           ├── VideoDetail.tsx        # 영상 상세
│           └── OpportunityFinder.tsx  # 기회 발견 (Pro)
│
├── ⚛️ 소스 코드 (src/)
│   ├── main.tsx                  # 앱 진입점 ⭐
│   ├── App.tsx                   # 메인 앱 & 라우팅 ⭐
│   ├── routes.tsx                # 라우트 경로 상수
│   │
│   ├── components/               # src 전용 컴포넌트
│   │   ├── alerts/
│   │   │   ├── EmptyState.tsx    # 빈 상태 UI
│   │   │   └── ErrorBoundary.tsx # 에러 경계
│   │   ├── channel/
│   │   │   ├── KpiCards.tsx      # KPI 지표
│   │   │   ├── ParetoChart.tsx   # 파레토 차트
│   │   │   ├── UploadHeatmap.tsx # 업로드 히트맵
│   │   │   └── VideoTable.tsx    # 영상 테이블
│   │   ├── guards/
│   │   │   ├── KeyGuard.tsx      # API 키 보호
│   │   │   └── ProGuard.tsx      # Pro 티어 보호
│   │   ├── ApiKeyGuide.tsx       # API 키 가이드
│   │   ├── NetworkBanner.tsx     # 네트워크 상태
│   │   └── icons.tsx             # SVG 아이콘
│   │
│   ├── hooks/                    # 커스텀 훅
│   │   ├── useHistory.ts         # 검색 히스토리
│   │   ├── useHotkeys.ts         # 키보드 단축키
│   │   ├── useTelemetry.ts       # 페이지뷰 추적
│   │   └── useTheme.ts           # 테마 관리
│   │
│   ├── lib/                      # 라이브러리
│   │   ├── simple-router.tsx     # ⭐ 자체 라우터
│   │   ├── secure-storage.ts     # 암호화 스토리지
│   │   ├── aggregate.ts          # 데이터 집계
│   │   ├── export.ts             # CSV/JSON 내보내기
│   │   ├── identify-channel.ts   # 채널 ID 추출
│   │   ├── rising-score.ts       # 라이징 스타 점수
│   │   ├── blue-ocean.ts         # 블루오션 분석
│   │   ├── history.ts            # 히스토리 관리
│   │   └── telemetry.ts          # 텔레메트리
│   │
│   └── prompts/
│       └── index.ts              # AI 프롬프트
│
└── 🚀 빌드 결과 (생성됨)
    ├── node_modules/             # 설치된 패키지
    ├── dist/                     # 빌드된 파일
    └── package-lock.json         # 정확한 버전 잠금
```

### 3-2. 핵심 파일별 역할

#### ⭐ 최우선 파일들

| 파일 | 역할 | 수정 빈도 |
|------|------|-----------|
| `src/main.tsx` | 앱 진입점, React DOM 렌더링 | 거의 없음 |
| `src/App.tsx` | 메인 앱, 라우팅 설정 | 중간 |
| `components/Layout.tsx` | 레이아웃, 네비게이션 | 높음 |
| `services/youtube-api.ts` | YouTube API 로직 | 높음 |
| `styles/globals.css` | 디자인 시스템 | 중간 |

#### 📄 src/main.tsx - 앱 진입점

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import '../styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**역할:**
- HTML의 `<div id="root">`에 React 앱 마운트
- 전역 CSS 임포트
- Strict Mode 활성화 (개발 중 문제 감지)

#### 📄 src/App.tsx - 메인 앱

```typescript
import { Router, Route, Switch } from './lib/simple-router';
import { Layout } from '../components/Layout';
import { Home } from '../components/Home';
// ... 기타 import

function App() {
  useTelemetry(); // 페이지뷰 추적

  return (
    <ErrorBoundary>
      <Router>
        <Layout>
          <Switch>
            <Route path="/" component={Home} />
            <Route path="/setup" component={ApiKeySetup} />
            
            {/* API 키 필요 페이지 */}
            <Route path="/channel">
              <KeyGuard>
                <ChannelAnalysis />
              </KeyGuard>
            </Route>
            
            {/* Pro 티어 필요 페이지 */}
            <Route path="/opportunity">
              <KeyGuard>
                <ProGuard>
                  <OpportunityFinder />
                </ProGuard>
              </KeyGuard>
            </Route>
            
            {/* 404 */}
            <Route>
              <div>404 Not Found</div>
            </Route>
          </Switch>
        </Layout>
        <Toaster />
      </Router>
    </ErrorBoundary>
  );
}

export default App;
```

**역할:**
- 라우팅 구조 정의
- 가드(Guard) 설정으로 접근 제어
- 에러 경계 설정
- 전역 토스트 알림 설정

#### 📄 src/lib/simple-router.tsx - 자체 라우터

```typescript
// Hash 기반 라우팅 (예: #/channel)
// wouter를 대체하여 CDN 의존성 제거

export function Router({ children }) {
  const [path, setPath] = useState(() => {
    return window.location.hash.slice(1) || '/';
  });

  useEffect(() => {
    const handleHashChange = () => {
      setPath(window.location.hash.slice(1) || '/');
    };
    window.addEventListener('hashchange', handleHashChange);
    return () => window.removeEventListener('hashchange', handleHashChange);
  }, []);

  // ...
}
```

**특징:**
- Hash 기반 라우팅 (#/path)
- 외부 의존성 없음
- History API 대신 hashchange 이벤트 사용

#### 📄 services/youtube-api.ts - YouTube API

```typescript
const YOUTUBE_API_BASE = 'https://www.googleapis.com/youtube/v3';

export async function getChannelInfo(channelId: string, apiKey: string) {
  const response = await fetch(
    `${YOUTUBE_API_BASE}/channels?` +
    `part=snippet,statistics,contentDetails&` +
    `id=${channelId}&` +
    `key=${apiKey}`
  );
  
  if (!response.ok) {
    throw new Error('Failed to fetch channel info');
  }
  
  const data = await response.json();
  return data.items[0];
}

// 기타 API 함수들...
```

**주요 함수들:**
- `getChannelInfo()` - 채널 기본 정보
- `searchVideos()` - 영상 검색
- `getVideoDetails()` - 영상 상세 정보
- `getChannelVideos()` - 채널의 모든 영상

### 3-3. 데이터 흐름 이해

```
사용자 입력
    ↓
UI 컴포넌트 (components/)
    ↓
커스텀 훅 (hooks/)
    ↓
API 서비스 (services/youtube-api.ts)
    ↓
YouTube Data API v3
    ↓
데이터 가공 (lib/aggregate.ts)
    ↓
로컬 스토리지 저장 (lib/storage.ts)
    ↓
UI 업데이트
```

---

## 4. 개발 환경 구축

### 4-1. 개발 서버 최초 실행

```bash
# 프로젝트 루트에서 실행
npm run dev
```

**실행 결과:**

```
  VITE v5.0.11  ready in 423 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

**브라우저에서 확인:**

1. 브라우저 열기 (Chrome 권장)
2. 주소창에 `http://localhost:5173` 입력
3. 홈 페이지가 로드되는지 확인

### 4-2. 개발 서버 명령어

```bash
# 개발 서버 시작
npm run dev

# 개발 서버 중지
# 터미널에서 Ctrl + C

# 다른 포트로 시작
npm run dev -- --port 3000

# 네트워크에 노출 (모바일 테스트용)
npm run dev -- --host
```

### 4-3. VS Code / Cursor 설정

#### 권장 확장 프로그램

VS Code나 Cursor에서 설치:

1. **필수 확장:**
   - ESLint - 코드 품질 검사
   - Prettier - 코드 포맷팅
   - Tailwind CSS IntelliSense - Tailwind 자동완성

2. **추천 확장:**
   - TypeScript Vue Plugin - TypeScript 지원
   - Error Lens - 에러를 코드에 인라인 표시
   - Auto Rename Tag - HTML 태그 자동 수정
   - Path Intellisense - 파일 경로 자동완성

#### .vscode/settings.json 생성

```json
{
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "tailwindCSS.experimental.classRegex": [
    ["cva\\(([^)]*)\\)", "[\"'`]([^\"'`]*).*?[\"'`]"],
    ["cx\\(([^)]*)\\)", "(?:'|\"|`)([^']*)(?:'|\"|`)"]
  ]
}
```

### 4-4. Git 초기화 (아직 안 했다면)

```bash
# Git 저장소 초기화
git init

# .gitignore 확인
cat .gitignore

# 첫 커밋
git add .
git commit -m "Initial commit: YouTube Channel Consultant"

# GitHub 연결 (저장소 생성 후)
git remote add origin https://github.com/사용자명/저장소명.git
git branch -M main
git push -u origin main
```

---

## 5. 단계별 개발 순서

### 전체 개발 로드맵

```
Phase 1: 기초 설정 (1일)
  ├── 환경 구축 ✓
  ├── YouTube API 키 발급
  └── 기본 동작 확인

Phase 2: 코어 기능 개발 (3-5일)
  ├── API 키 관리 시스템
  ├── 채널 분석 기능
  ├── 키워드 분석 기능
  └── 데이터 시각화

Phase 3: 고급 기능 개발 (2-3일)
  ├── 영상 상세 분석
  ├── CSV/JSON 내보내기
  └── Pro 티어 기능

Phase 4: UI/UX 개선 (2-3일)
  ├── 반응형 디자인
  ├── 로딩 상태
  ├── 에러 처리
  └── 접근성

Phase 5: 테스트 & 최적화 (1-2일)
  ├── 기능 테스트
  ├── 성능 최적화
  └── 버그 수정

Phase 6: 배포 준비 (1일)
  ├── 프로덕션 빌드
  ├── 환경 변수 설정
  └── 배포

Total: 10-15일
```

### 5-1. Phase 1: 기초 설정 (1일)

#### Day 1 - Morning: YouTube API 키 발급

**1-1. Google Cloud Console 접속**

```
1. https://console.cloud.google.com 접속
2. Google 계정 로그인
3. 프로젝트 만들기
   - 프로젝트 이름: "YouTube-Channel-Consultant"
   - 위치: 조직 없음
```

**1-2. YouTube Data API v3 활성화**

```
1. 왼쪽 메뉴 → "API 및 서비스" → "라이브러리"
2. 검색: "YouTube Data API v3"
3. 첫 번째 결과 클릭
4. "사용 설정" 버튼 클릭
5. 활성화 확인 (1-2분 소요)
```

**1-3. API 키 생성**

```
1. 왼쪽 메뉴 → "API 및 서비스" → "사용자 인증 정보"
2. 상단 "+ 사용자 인증 정보 만들기" 클릭
3. "API 키" 선택
4. API 키 생성됨 (예: AIzaSyB1234567890...)
5. 복사해서 안전한 곳에 저장
```

**1-4. API 키 제한 설정 (보안)**

```
1. 생성된 API 키 옆 "제한사항 수정" 클릭
2. API 제한사항:
   - "키 제한" 선택
   - "YouTube Data API v3"만 체크
3. 저장
```

#### Day 1 - Afternoon: 앱 기본 동작 확인

**1-5. 개발 서버 실행**

```bash
npm run dev
```

**1-6. 홈 페이지 확인**

```
브라우저: http://localhost:5173
확인 사항:
- [ ] 페이지가 로드되는가?
- [ ] 네비게이션 메뉴가 보이는가?
- [ ] 스타일이 제대로 적용되는가?
- [ ] 콘솔 에러가 없는가?
```

**1-7. API 키 설정 페이지 테스트**

```
1. "API 키 설정" 메뉴 클릭
2. 발급받은 API 키 입력
3. "저장" 버튼 클릭
4. 성공 메시지 확인
5. 로컬 스토리지 확인:
   - F12 → Application → Local Storage
   - "youtube_api_key" 키 확인
```

**1-8. 기본 채널 분석 테스트**

```
1. "채널 분석" 메뉴 클릭
2. 테스트 채널 URL 입력:
   https://www.youtube.com/@YouTube
3. "분석 시작" 클릭
4. 로딩 확인
5. 결과 페이지 확인:
   - [ ] 채널 정보 표시
   - [ ] 구독자, 영상 수 등 통계
   - [ ] 에러 없음
```

✅ **Phase 1 완료 체크리스트:**
- [ ] YouTube API 키 발급 완료
- [ ] 개발 서버 정상 실행
- [ ] API 키 설정 기능 동작
- [ ] 기본 채널 분석 동작

### 5-2. Phase 2: 코어 기능 개발 (3-5일)

#### Day 2: API 키 관리 시스템 개선

**목표:** API 키를 안전하게 저장하고 관리하는 시스템 구축

**2-1. 파일 확인: `hooks/useApiKey.ts`**

```bash
# 파일 열기
code hooks/useApiKey.ts

# 또는 Cursor에서
cursor hooks/useApiKey.ts
```

**현재 코드 분석:**

```typescript
import { useState, useEffect } from 'react';
import { storage } from '../lib/storage';

const API_KEY_STORAGE_KEY = 'youtube_api_key';

export function useApiKey() {
  const [apiKey, setApiKey] = useState<string>('');
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    // 로컬 스토리지에서 API 키 불러오기
    const saved = storage.get(API_KEY_STORAGE_KEY);
    if (saved) {
      setApiKey(saved);
    }
    setIsLoading(false);
  }, []);

  const saveApiKey = (key: string) => {
    storage.set(API_KEY_STORAGE_KEY, key);
    setApiKey(key);
  };

  const clearApiKey = () => {
    storage.remove(API_KEY_STORAGE_KEY);
    setApiKey('');
  };

  const validateApiKey = async (key: string): Promise<boolean> => {
    // YouTube API 호출로 키 유효성 검증
    try {
      const response = await fetch(
        `https://www.googleapis.com/youtube/v3/channels?` +
        `part=snippet&id=UC_x5XG1OV2P6uZZ5FSM9Ttw&key=${key}`
      );
      return response.ok;
    } catch {
      return false;
    }
  };

  return {
    apiKey,
    isLoading,
    saveApiKey,
    clearApiKey,
    validateApiKey,
    hasApiKey: !!apiKey,
  };
}
```

**개선 작업:**

```typescript
// src/lib/secure-storage.ts 확인
// 암호화된 스토리지 사용 여부 확인

import { secureStorage } from '../lib/secure-storage';

// API 키를 암호화해서 저장
const saveApiKey = (key: string) => {
  secureStorage.set(API_KEY_STORAGE_KEY, key);
  setApiKey(key);
  
  // 저장 성공 이벤트 발생
  window.dispatchEvent(new CustomEvent('apikey:saved'));
};
```

**테스트:**

```bash
# 개발 서버 실행 중인 상태에서
# 브라우저 콘솔에서 테스트

// API 키 저장 테스트
localStorage.setItem('youtube_api_key', 'test_key_123');

// 새로고침 후 유지 확인
location.reload();

// 콘솔에서 확인
localStorage.getItem('youtube_api_key'); // 'test_key_123'
```

#### Day 3: 채널 분석 기능 개발

**목표:** 채널 URL 입력 → 분석 → 결과 표시

**3-1. 채널 ID 추출 로직 (`src/lib/identify-channel.ts`)**

```typescript
export function extractChannelId(input: string): {
  type: 'id' | 'username' | 'url' | 'handle';
  value: string;
} | null {
  // URL에서 채널 ID 추출
  const patterns = [
    // https://www.youtube.com/channel/UCxxxxxxx
    /youtube\.com\/channel\/([^/?]+)/,
    
    // https://www.youtube.com/@username
    /youtube\.com\/@([^/?]+)/,
    
    // https://www.youtube.com/c/username
    /youtube\.com\/c\/([^/?]+)/,
    
    // https://www.youtube.com/user/username
    /youtube\.com\/user\/([^/?]+)/,
  ];

  for (const pattern of patterns) {
    const match = input.match(pattern);
    if (match) {
      return {
        type: input.includes('/channel/') ? 'id' : 'username',
        value: match[1],
      };
    }
  }

  // 직접 ID 입력 (UC로 시작)
  if (input.startsWith('UC') && input.length === 24) {
    return { type: 'id', value: input };
  }

  return null;
}
```

**테스트:**

```typescript
// 브라우저 콘솔에서
import { extractChannelId } from './src/lib/identify-channel';

// 테스트 케이스
extractChannelId('https://www.youtube.com/@YouTube');
// { type: 'username', value: 'YouTube' }

extractChannelId('https://www.youtube.com/channel/UC_x5XG1OV2P6uZZ5FSM9Ttw');
// { type: 'id', value: 'UC_x5XG1OV2P6uZZ5FSM9Ttw' }

extractChannelId('UC_x5XG1OV2P6uZZ5FSM9Ttw');
// { type: 'id', value: 'UC_x5XG1OV2P6uZZ5FSM9Ttw' }
```

**3-2. YouTube API 호출 (`services/youtube-api.ts`)**

```typescript
// 채널 정보 가져오기
export async function getChannelInfo(channelId: string, apiKey: string) {
  const url = new URL('https://www.googleapis.com/youtube/v3/channels');
  url.searchParams.set('part', 'snippet,statistics,contentDetails');
  url.searchParams.set('id', channelId);
  url.searchParams.set('key', apiKey);

  const response = await fetch(url.toString());

  if (!response.ok) {
    const error = await response.json();
    throw new Error(error.error?.message || 'Failed to fetch channel');
  }

  const data = await response.json();
  
  if (!data.items || data.items.length === 0) {
    throw new Error('Channel not found');
  }

  return data.items[0];
}

// 채널의 영상 목록 가져오기
export async function getChannelVideos(
  channelId: string,
  apiKey: string,
  maxResults: number = 50
) {
  // 1단계: 채널의 업로드 재생목록 ID 가져오기
  const channelInfo = await getChannelInfo(channelId, apiKey);
  const uploadsPlaylistId = channelInfo.contentDetails.relatedPlaylists.uploads;

  // 2단계: 재생목록의 영상들 가져오기
  const url = new URL('https://www.googleapis.com/youtube/v3/playlistItems');
  url.searchParams.set('part', 'snippet');
  url.searchParams.set('playlistId', uploadsPlaylistId);
  url.searchParams.set('maxResults', maxResults.toString());
  url.searchParams.set('key', apiKey);

  const response = await fetch(url.toString());
  const data = await response.json();

  if (!response.ok) {
    throw new Error(data.error?.message || 'Failed to fetch videos');
  }

  // 3단계: 각 영상의 상세 정보 가져오기 (조회수, 좋아요 등)
  const videoIds = data.items.map((item: any) => 
    item.snippet.resourceId.videoId
  ).join(',');

  const detailsUrl = new URL('https://www.googleapis.com/youtube/v3/videos');
  detailsUrl.searchParams.set('part', 'statistics,contentDetails');
  detailsUrl.searchParams.set('id', videoIds);
  detailsUrl.searchParams.set('key', apiKey);

  const detailsResponse = await fetch(detailsUrl.toString());
  const detailsData = await detailsResponse.json();

  // 데이터 병합
  return data.items.map((item: any, index: number) => ({
    id: item.snippet.resourceId.videoId,
    title: item.snippet.title,
    description: item.snippet.description,
    publishedAt: item.snippet.publishedAt,
    thumbnailUrl: item.snippet.thumbnails.high.url,
    statistics: detailsData.items[index]?.statistics || {},
    duration: detailsData.items[index]?.contentDetails?.duration || '',
  }));
}
```

**3-3. 채널 분석 컴포넌트 (`components/ChannelAnalysis.tsx`)**

```typescript
import { useState } from 'react';
import { useApiKey } from '../hooks/useApiKey';
import { extractChannelId } from '../src/lib/identify-channel';
import { getChannelInfo } from '../services/youtube-api';
import { useRouter } from '../src/lib/simple-router';
import { toast } from 'sonner';

export function ChannelAnalysis() {
  const [input, setInput] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  const { apiKey } = useApiKey();
  const { navigate } = useRouter();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();

    // 1. 입력 검증
    if (!input.trim()) {
      toast.error('채널 URL 또는 ID를 입력해주세요');
      return;
    }

    // 2. 채널 ID 추출
    const channelData = extractChannelId(input);
    if (!channelData) {
      toast.error('올바른 YouTube 채널 URL을 입력해주세요');
      return;
    }

    setIsLoading(true);

    try {
      // 3. API 호출
      let channelId = channelData.value;

      // username인 경우 ID로 변환 필요
      if (channelData.type === 'username') {
        // search API로 채널 ID 찾기
        const searchUrl = new URL(
          'https://www.googleapis.com/youtube/v3/search'
        );
        searchUrl.searchParams.set('part', 'snippet');
        searchUrl.searchParams.set('q', channelData.value);
        searchUrl.searchParams.set('type', 'channel');
        searchUrl.searchParams.set('maxResults', '1');
        searchUrl.searchParams.set('key', apiKey);

        const searchRes = await fetch(searchUrl.toString());
        const searchData = await searchRes.json();

        if (searchData.items && searchData.items.length > 0) {
          channelId = searchData.items[0].snippet.channelId;
        } else {
          throw new Error('채널을 찾을 수 없습니다');
        }
      }

      // 4. 채널 정보 가져오기
      const channelInfo = await getChannelInfo(channelId, apiKey);

      // 5. 결과 페이지로 이동
      navigate(`/channel/detail?id=${channelId}`);

    } catch (error: any) {
      console.error('채널 분석 실패:', error);
      toast.error(error.message || '채널 정보를 가져올 수 없습니다');
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="max-w-2xl mx-auto p-6">
      <h1 className="text-3xl mb-6">채널 분석</h1>

      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label className="block mb-2">
            YouTube 채널 URL 또는 ID
          </label>
          <input
            type="text"
            value={input}
            onChange={(e) => setInput(e.target.value)}
            placeholder="https://www.youtube.com/@YouTube"
            className="w-full px-4 py-2 border rounded-lg"
            disabled={isLoading}
          />
        </div>

        <button
          type="submit"
          disabled={isLoading}
          className="w-full bg-red-600 text-white py-3 rounded-lg
                     hover:bg-red-700 disabled:opacity-50"
        >
          {isLoading ? '분석 중...' : '분석 시작'}
        </button>
      </form>

      <div className="mt-6 text-sm text-gray-500">
        <p>지원하는 형식:</p>
        <ul className="list-disc list-inside mt-2 space-y-1">
          <li>https://www.youtube.com/@username</li>
          <li>https://www.youtube.com/channel/UC...</li>
          <li>UC로 시작하는 채널 ID</li>
        </ul>
      </div>
    </div>
  );
}
```

**3-4. 채널 상세 페이지 (`components/ChannelDetail.tsx`)**

```typescript
import { useState, useEffect } from 'react';
import { useApiKey } from '../hooks/useApiKey';
import { getChannelInfo, getChannelVideos } from '../services/youtube-api';
import { KpiCards } from '../src/components/channel/KpiCards';
import { VideoTable } from '../src/components/channel/VideoTable';
import { EmptyState } from '../src/components/alerts/EmptyState';

export function ChannelDetail() {
  const [channelData, setChannelData] = useState<any>(null);
  const [videos, setVideos] = useState<any[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState('');
  const { apiKey } = useApiKey();

  // URL에서 채널 ID 가져오기
  const channelId = new URLSearchParams(window.location.hash.split('?')[1])
    .get('id');

  useEffect(() => {
    if (!channelId || !apiKey) return;

    const fetchData = async () => {
      setIsLoading(true);
      setError('');

      try {
        // 채널 정보와 영상 목록 병렬 로드
        const [channel, videoList] = await Promise.all([
          getChannelInfo(channelId, apiKey),
          getChannelVideos(channelId, apiKey, 50),
        ]);

        setChannelData(channel);
        setVideos(videoList);
      } catch (err: any) {
        setError(err.message);
      } finally {
        setIsLoading(false);
      }
    };

    fetchData();
  }, [channelId, apiKey]);

  if (isLoading) {
    return (
      <div className="flex items-center justify-center h-64">
        <div className="animate-spin rounded-full h-12 w-12 
                        border-4 border-red-600 border-t-transparent" />
      </div>
    );
  }

  if (error) {
    return (
      <EmptyState
        title="에러가 발생했습니다"
        description={error}
        action={{
          label: '다시 시도',
          onClick: () => window.location.reload(),
        }}
      />
    );
  }

  if (!channelData) {
    return <EmptyState title="채널을 찾을 수 없습니다" />;
  }

  return (
    <div className="space-y-6">
      {/* 채널 헤더 */}
      <div className="flex items-start gap-4">
        <img
          src={channelData.snippet.thumbnails.high.url}
          alt={channelData.snippet.title}
          className="w-24 h-24 rounded-full"
        />
        <div>
          <h1 className="text-3xl">{channelData.snippet.title}</h1>
          <p className="text-gray-500 mt-2">
            {channelData.snippet.description}
          </p>
        </div>
      </div>

      {/* KPI 카드 */}
      <KpiCards
        subscribers={channelData.statistics.subscriberCount}
        videos={channelData.statistics.videoCount}
        views={channelData.statistics.viewCount}
      />

      {/* 영상 목록 */}
      <VideoTable videos={videos} />
    </div>
  );
}
```

**테스트 체크리스트:**

```
✅ 테스트 시나리오:

1. 채널 URL 입력 테스트
   - [ ] @username 형식
   - [ ] /channel/UC... 형식
   - [ ] 직접 ID 입력

2. 로딩 상태
   - [ ] 로딩 스피너 표시
   - [ ] 버튼 비활성화

3. 성공 케이스
   - [ ] 채널 정보 표시
   - [ ] KPI 카드 렌더링
   - [ ] 영상 목록 표시

4. 에러 케이스
   - [ ] 잘못된 URL
   - [ ] 존재하지 않는 채널
   - [ ] API 키 오류
   - [ ] 네트워크 오류
```

#### Day 4-5: 데이터 시각화

**목표:** 채널 데이터를 차트로 시각화

**4-1. KPI 카드 컴포넌트 (`src/components/channel/KpiCards.tsx`)**

```typescript
interface KpiCardsProps {
  subscribers: string;
  videos: string;
  views: string;
}

export function KpiCards({ subscribers, videos, views }: KpiCardsProps) {
  const formatNumber = (num: string) => {
    const n = parseInt(num);
    if (n >= 1000000) return `${(n / 1000000).toFixed(1)}M`;
    if (n >= 1000) return `${(n / 1000).toFixed(1)}K`;
    return n.toLocaleString();
  };

  const kpis = [
    {
      label: '구독자',
      value: formatNumber(subscribers),
      icon: '👥',
      color: 'bg-blue-500',
    },
    {
      label: '영상 수',
      value: formatNumber(videos),
      icon: '🎬',
      color: 'bg-green-500',
    },
    {
      label: '총 조회수',
      value: formatNumber(views),
      icon: '👁️',
      color: 'bg-purple-500',
    },
  ];

  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
      {kpis.map((kpi) => (
        <div
          key={kpi.label}
          className="bg-slate-800 rounded-lg p-6 border border-slate-700"
        >
          <div className="flex items-center justify-between mb-2">
            <span className="text-gray-400 text-sm">{kpi.label}</span>
            <span className="text-2xl">{kpi.icon}</span>
          </div>
          <div className="text-3xl">{kpi.value}</div>
        </div>
      ))}
    </div>
  );
}
```

**4-2. 파레토 차트 (`src/components/channel/ParetoChart.tsx`)**

파레토 법칙 (20%의 영상이 80%의 조회수 생성)을 시각화:

```typescript
interface Video {
  id: string;
  title: string;
  statistics: {
    viewCount: string;
  };
}

interface ParetoChartProps {
  videos: Video[];
}

export function ParetoChart({ videos }: ParetoChartProps) {
  // 조회수로 정렬
  const sorted = [...videos].sort(
    (a, b) => parseInt(b.statistics.viewCount) - 
              parseInt(a.statistics.viewCount)
  );

  // 전체 조회수
  const totalViews = sorted.reduce(
    (sum, v) => sum + parseInt(v.statistics.viewCount), 0
  );

  // 누적 퍼센트 계산
  let cumulative = 0;
  const chartData = sorted.map((video, index) => {
    const views = parseInt(video.statistics.viewCount);
    cumulative += views;
    const cumulativePercent = (cumulative / totalViews) * 100;

    return {
      index: index + 1,
      title: video.title,
      views,
      cumulativePercent,
    };
  });

  // 20%의 영상을 찾기
  const twentyPercentIndex = Math.ceil(videos.length * 0.2);
  const twentyPercentViews = chartData[twentyPercentIndex - 1]?.cumulativePercent || 0;

  return (
    <div className="bg-slate-800 rounded-lg p-6 border border-slate-700">
      <h3 className="text-xl mb-4">파레토 분석</h3>
      
      <div className="mb-4 p-4 bg-slate-700 rounded">
        <p className="text-sm text-gray-300">
          상위 20% 영상({twentyPercentIndex}개)이 전체 조회수의{' '}
          <span className="text-red-500 font-bold">
            {twentyPercentViews.toFixed(1)}%
          </span>
          를 차지합니다
        </p>
      </div>

      {/* CSS 기반 차트 */}
      <div className="space-y-2">
        {chartData.slice(0, 20).map((item, index) => (
          <div key={item.index} className="flex items-center gap-2">
            <div className="w-8 text-xs text-gray-400">
              #{item.index}
            </div>
            
            {/* 바 차트 */}
            <div className="flex-1 bg-slate-700 rounded-full h-6 relative overflow-hidden">
              <div
                className="h-full bg-gradient-to-r from-red-600 to-red-400 
                           transition-all duration-500"
                style={{ width: `${item.cumulativePercent}%` }}
              />
              
              <div className="absolute inset-0 flex items-center px-2">
                <span className="text-xs text-white truncate">
                  {item.title}
                </span>
              </div>
            </div>

            <div className="w-16 text-xs text-gray-400 text-right">
              {item.cumulativePercent.toFixed(0)}%
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

**4-3. 업로드 히트맵 (`src/components/channel/UploadHeatmap.tsx`)**

요일/시간대별 업로드 패턴 분석:

```typescript
interface UploadHeatmapProps {
  videos: Array<{ publishedAt: string }>;
}

export function UploadHeatmap({ videos }: UploadHeatmapProps) {
  // 요일/시간대별 업로드 횟수 집계
  const heatmapData = new Array(7).fill(null).map(() => new Array(24).fill(0));

  videos.forEach((video) => {
    const date = new Date(video.publishedAt);
    const day = date.getDay(); // 0 = 일요일
    const hour = date.getHours();
    heatmapData[day][hour]++;
  });

  // 최대값 찾기 (색상 정규화용)
  const maxCount = Math.max(...heatmapData.flat());

  const days = ['일', '월', '화', '수', '목', '금', '토'];

  return (
    <div className="bg-slate-800 rounded-lg p-6 border border-slate-700">
      <h3 className="text-xl mb-4">업로드 히트맵</h3>

      <div className="overflow-x-auto">
        <div className="inline-block min-w-full">
          {/* 시간 헤더 */}
          <div className="flex mb-1">
            <div className="w-12" /> {/* 요일 공간 */}
            {Array.from({ length: 24 }, (_, i) => (
              <div key={i} className="w-8 text-xs text-center text-gray-400">
                {i}
              </div>
            ))}
          </div>

          {/* 히트맵 */}
          {heatmapData.map((dayData, dayIndex) => (
            <div key={dayIndex} className="flex mb-1">
              <div className="w-12 text-sm text-gray-400 flex items-center">
                {days[dayIndex]}
              </div>
              {dayData.map((count, hour) => {
                const intensity = maxCount > 0 ? count / maxCount : 0;
                const opacity = 0.2 + intensity * 0.8;

                return (
                  <div
                    key={hour}
                    className="w-8 h-8 mr-1 rounded flex items-center justify-center
                               cursor-pointer transition-all hover:scale-110"
                    style={{
                      backgroundColor: `rgba(239, 68, 68, ${opacity})`,
                    }}
                    title={`${days[dayIndex]} ${hour}시: ${count}개`}
                  >
                    {count > 0 && (
                      <span className="text-xs text-white">{count}</span>
                    )}
                  </div>
                );
              })}
            </div>
          ))}
        </div>
      </div>

      <div className="mt-4 text-sm text-gray-400">
        💡 가장 많이 업로드한 시간대를 확인하여 최적의 업로드 시간을 파악하세요
      </div>
    </div>
  );
}
```

**테스트:**

```
브라우저에서 확인:
1. KPI 카드 렌더링
2. 파레토 차트 애니메이션
3. 히트맵 색상 그라데이션
4. 호버 효과
5. 반응형 레이아웃
```

✅ **Phase 2 완료 체크리스트:**
- [ ] API 키 관리 시스템 구현
- [ ] 채널 분석 기능 완성
- [ ] 데이터 시각화 구현
- [ ] 에러 처리 추가

### 5-3. Phase 3: 고급 기능 개발 (2-3일)

#### Day 6: 키워드 분석 기능

**목표:** 키워드로 영상 검색 및 분석

**6-1. 키워드 분석 컴포넌트 (`components/KeywordAnalysis.tsx`)**

```typescript
import { useState } from 'react';
import { useApiKey } from '../hooks/useApiKey';
import { toast } from 'sonner';

export function KeywordAnalysis() {
  const [keyword, setKeyword] = useState('');
  const [results, setResults] = useState<any[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const { apiKey } = useApiKey();

  const handleSearch = async (e: React.FormEvent) => {
    e.preventDefault();

    if (!keyword.trim()) {
      toast.error('키워드를 입력해주세요');
      return;
    }

    setIsLoading(true);

    try {
      // YouTube Search API 호출
      const url = new URL('https://www.googleapis.com/youtube/v3/search');
      url.searchParams.set('part', 'snippet');
      url.searchParams.set('q', keyword);
      url.searchParams.set('type', 'video');
      url.searchParams.set('maxResults', '50');
      url.searchParams.set('order', 'viewCount');
      url.searchParams.set('key', apiKey);

      const response = await fetch(url.toString());
      const data = await response.json();

      if (!response.ok) {
        throw new Error(data.error?.message || '검색 실패');
      }

      // 영상 상세 정보 가져오기
      const videoIds = data.items.map((item: any) => item.id.videoId).join(',');
      
      const detailsUrl = new URL('https://www.googleapis.com/youtube/v3/videos');
      detailsUrl.searchParams.set('part', 'statistics,contentDetails');
      detailsUrl.searchParams.set('id', videoIds);
      detailsUrl.searchParams.set('key', apiKey);

      const detailsResponse = await fetch(detailsUrl.toString());
      const detailsData = await detailsResponse.json();

      // 데이터 병합
      const combined = data.items.map((item: any, index: number) => ({
        ...item,
        statistics: detailsData.items[index]?.statistics || {},
      }));

      setResults(combined);
      
      // 분석 결과 계산
      const totalViews = combined.reduce(
        (sum: number, item: any) => 
          sum + parseInt(item.statistics.viewCount || '0'), 0
      );
      const avgViews = totalViews / combined.length;

      toast.success(
        `${combined.length}개 영상 검색 완료 (평균 조회수: ${avgViews.toLocaleString()})`
      );

    } catch (error: any) {
      console.error('검색 실패:', error);
      toast.error(error.message);
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="space-y-6">
      <h1 className="text-3xl">키워드 분석</h1>

      <form onSubmit={handleSearch} className="flex gap-2">
        <input
          type="text"
          value={keyword}
          onChange={(e) => setKeyword(e.target.value)}
          placeholder="분석할 키워드 입력 (예: React 튜토리얼)"
          className="flex-1 px-4 py-2 bg-slate-800 border border-slate-700 
                     rounded-lg focus:outline-none focus:border-red-500"
          disabled={isLoading}
        />
        <button
          type="submit"
          disabled={isLoading}
          className="px-6 py-2 bg-red-600 text-white rounded-lg
                     hover:bg-red-700 disabled:opacity-50"
        >
          {isLoading ? '검색 중...' : '검색'}
        </button>
      </form>

      {/* 검색 결과 */}
      {results.length > 0 && (
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {results.map((video) => (
            <div
              key={video.id.videoId}
              className="bg-slate-800 border border-slate-700 rounded-lg 
                         overflow-hidden hover:border-red-500 transition-colors"
            >
              <img
                src={video.snippet.thumbnails.high.url}
                alt={video.snippet.title}
                className="w-full aspect-video object-cover"
              />
              <div className="p-4">
                <h3 className="line-clamp-2 mb-2">
                  {video.snippet.title}
                </h3>
                <div className="text-sm text-gray-400 space-y-1">
                  <div>
                    채널: {video.snippet.channelTitle}
                  </div>
                  <div>
                    조회수: {parseInt(video.statistics.viewCount || '0').toLocaleString()}
                  </div>
                  <div>
                    좋아요: {parseInt(video.statistics.likeCount || '0').toLocaleString()}
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

#### Day 7: CSV/JSON 내보내기

**목표:** 분석 데이터를 파일로 저장

**7-1. 내보내기 유틸리티 (`src/lib/export.ts`)**

```typescript
export function exportToCSV(data: any[], filename: string) {
  if (!data || data.length === 0) {
    throw new Error('No data to export');
  }

  // CSV 헤더
  const headers = Object.keys(data[0]);
  const csvHeaders = headers.join(',');

  // CSV 바디
  const csvRows = data.map((row) =>
    headers.map((header) => {
      const value = row[header];
      // 문자열 이스케이프
      if (typeof value === 'string') {
        return `"${value.replace(/"/g, '""')}"`;
      }
      return value;
    }).join(',')
  );

  const csv = [csvHeaders, ...csvRows].join('\n');

  // BOM 추가 (한글 깨짐 방지)
  const bom = '\uFEFF';
  const blob = new Blob([bom + csv], { type: 'text/csv;charset=utf-8;' });

  // 다운로드
  const link = document.createElement('a');
  const url = URL.createObjectURL(blob);
  link.setAttribute('href', url);
  link.setAttribute('download', `${filename}.csv`);
  link.style.visibility = 'hidden';
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
}

export function exportToJSON(data: any, filename: string) {
  const json = JSON.stringify(data, null, 2);
  const blob = new Blob([json], { type: 'application/json' });

  const link = document.createElement('a');
  const url = URL.createObjectURL(blob);
  link.setAttribute('href', url);
  link.setAttribute('download', `${filename}.json`);
  link.style.visibility = 'hidden';
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
}
```

**7-2. 내보내기 버튼 추가 (`components/ChannelDetail.tsx`)**

```typescript
import { exportToCSV, exportToJSON } from '../src/lib/export';

// 컴포넌트 내부에 추가
const handleExportCSV = () => {
  try {
    const exportData = videos.map((video) => ({
      제목: video.title,
      조회수: video.statistics.viewCount,
      좋아요: video.statistics.likeCount,
      댓글: video.statistics.commentCount,
      게시일: new Date(video.publishedAt).toLocaleDateString('ko-KR'),
    }));

    exportToCSV(exportData, `${channelData.snippet.title}_영상목록`);
    toast.success('CSV 파일로 내보내기 완료');
  } catch (error) {
    toast.error('내보내기 실패');
  }
};

// JSX에 버튼 추가
<div className="flex gap-2">
  <button
    onClick={handleExportCSV}
    className="px-4 py-2 bg-green-600 text-white rounded-lg
               hover:bg-green-700"
  >
    CSV 내보내기
  </button>
  
  <button
    onClick={() => exportToJSON(videos, `${channelData.snippet.title}_데이터`)}
    className="px-4 py-2 bg-blue-600 text-white rounded-lg
               hover:bg-blue-700"
  >
    JSON 내보내기
  </button>
</div>
```

✅ **Phase 3 완료 체크리스트:**
- [ ] 키워드 분석 기능
- [ ] CSV 내보내기
- [ ] JSON 내보내기
- [ ] Pro 티어 기능 (OpportunityFinder)

---

## 6. 각 기능별 개발 가이드

### 6-1. 라우팅 추가 방법

**새로운 페이지 추가 4단계:**

```typescript
// 1단계: 컴포넌트 생성
// components/NewPage.tsx
export function NewPage() {
  return <div>New Page</div>;
}

// 2단계: 라우트 경로 정의
// src/routes.tsx
export const routes = {
  // ... 기존 라우트
  newPage: '/new-page',
};

// 3단계: App.tsx에 라우트 추가
// src/App.tsx
import { NewPage } from '../components/NewPage';

<Switch>
  {/* ... 기존 라우트 */}
  <Route path="/new-page" component={NewPage} />
</Switch>

// 4단계: 네비게이션 메뉴 추가
// components/Layout.tsx
<Link href="/new-page">
  New Page
</Link>
```

### 6-2. API 함수 추가 방법

```typescript
// services/youtube-api.ts

export async function newApiFunction(
  param: string,
  apiKey: string
): Promise<ResultType> {
  // 1. URL 구성
  const url = new URL('https://www.googleapis.com/youtube/v3/endpoint');
  url.searchParams.set('part', 'snippet');
  url.searchParams.set('param', param);
  url.searchParams.set('key', apiKey);

  // 2. fetch 호출
  const response = await fetch(url.toString());

  // 3. 에러 처리
  if (!response.ok) {
    const error = await response.json();
    throw new Error(error.error?.message || 'API call failed');
  }

  // 4. 데이터 반환
  const data = await response.json();
  return data.items;
}
```

### 6-3. 새로운 훅 생성 방법

```typescript
// src/hooks/useCustomHook.ts

import { useState, useEffect } from 'react';

export function useCustomHook(param: string) {
  const [data, setData] = useState<any>(null);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string>('');

  useEffect(() => {
    // 비동기 로직
    const fetchData = async () => {
      setIsLoading(true);
      setError('');

      try {
        // API 호출 등
        const result = await someAsyncFunction(param);
        setData(result);
      } catch (err: any) {
        setError(err.message);
      } finally {
        setIsLoading(false);
      }
    };

    if (param) {
      fetchData();
    }
  }, [param]);

  return { data, isLoading, error };
}

// 사용 예시
function MyComponent() {
  const { data, isLoading, error } = useCustomHook('param');

  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error: {error}</div>;
  
  return <div>{JSON.stringify(data)}</div>;
}
```

---

## 7. 테스트 및 디버깅

### 7-1. 기능 테스트 체크리스트

#### API 키 관리
```
- [ ] API 키 저장
- [ ] API 키 불러오기
- [ ] API 키 삭제
- [ ] API 키 유효성 검증
- [ ] 페이지 새로고침 후 유지
```

#### 채널 분석
```
- [ ] @username 형식 입력
- [ ] /channel/UC... 형식 입력
- [ ] 직접 ID 입력
- [ ] 존재하지 않는 채널 처리
- [ ] API 에러 처리
- [ ] 로딩 상태 표시
```

#### 데이터 시각화
```
- [ ] KPI 카드 렌더링
- [ ] 파레토 차트 계산 정확성
- [ ] 히트맵 색상 표시
- [ ] 반응형 레이아웃
```

### 7-2. 브라우저 콘솔 디버깅

```javascript
// 1. 로컬 스토리지 확인
localStorage.getItem('youtube_api_key');

// 2. 현재 경로 확인
console.log(window.location.hash);

// 3. React 컴포넌트 디버깅 (React DevTools 설치 필요)
// Chrome 확장 프로그램에서 React DevTools 설치
// F12 → React 탭에서 컴포넌트 상태 확인

// 4. API 응답 확인
fetch('https://www.googleapis.com/youtube/v3/channels?part=snippet&id=UC...&key=YOUR_KEY')
  .then(r => r.json())
  .then(console.log);
```

### 7-3. 일반적인 에러 해결

#### "API key not valid"
```
원인: API 키가 잘못되었거나 만료됨
해결:
1. Google Cloud Console에서 키 확인
2. YouTube Data API v3 활성화 확인
3. API 제한사항 확인
4. 새 키 생성
```

#### "Failed to fetch"
```
원인: 네트워크 문제 또는 CORS
해결:
1. 인터넷 연결 확인
2. API URL 확인
3. 브라우저 콘솔에서 정확한 에러 확인
```

#### "Cannot read property of undefined"
```
원인: 데이터가 아직 로드되지 않음
해결:
1. 옵셔널 체이닝 사용: data?.property
2. 로딩 상태 확인 추가
3. 기본값 설정: data || []
```

---

## 8. 빌드 및 배포 준비

### 8-1. 프로덕션 빌드 테스트

```bash
# 1. 빌드 실행
npm run build

# 출력 예시:
# vite v5.0.11 building for production...
# ✓ 234 modules transformed.
# dist/index.html                   0.45 kB │ gzip:  0.30 kB
# dist/assets/index-a1b2c3d4.css   12.34 kB │ gzip:  3.45 kB
# dist/assets/index-a1b2c3d4.js   234.56 kB │ gzip: 78.90 kB
# ✓ built in 3.45s

# 2. 빌드 결과 확인
ls -la dist/

# 3. 로컬에서 빌드 미리보기
npm run preview

# 4. 브라우저에서 확인
# http://localhost:4173
```

### 8-2. 배포 전 체크리스트

```
코드 품질:
- [ ] TypeScript 에러 없음 (`tsc --noEmit`)
- [ ] 콘솔 에러 없음
- [ ] 콘솔 경고 최소화
- [ ] console.log 제거

성능:
- [ ] 빌드 크기 확인 (dist 폴더)
- [ ] 불필요한 의존성 제거
- [ ] 이미지 최적화
- [ ] 코드 스플리팅 확인

보안:
- [ ] API 키 하드코딩 제거
- [ ] 민감한 정보 .gitignore 추가
- [ ] HTTPS 사용

기능:
- [ ] 모든 페이지 동작 확인
- [ ] 모바일 반응형 확인
- [ ] 주요 브라우저 테스트

SEO:
- [ ] meta 태그 추가
- [ ] og:image 설정
- [ ] 적절한 title 설정
```

### 8-3. vercel.json 설정

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
```

---

## 9. 배포 실행

### 9-1. Vercel 배포 (가장 쉬움)

```bash
# 1. Vercel CLI 설치 (선택)
npm i -g vercel

# 2. Vercel 로그인
vercel login

# 3. 배포
vercel

# 또는 GUI 방식:
# 1. https://vercel.com 접속
# 2. GitHub 연결
# 3. 저장소 선택
# 4. "Deploy" 클릭
```

상세한 배포 과정은 `/배포_완벽_가이드.md` 참조

### 9-2. 배포 후 확인

```
✅ 체크리스트:
- [ ] 사이트 접속 가능
- [ ] 모든 페이지 동작
- [ ] API 호출 정상
- [ ] 스타일 적용
- [ ] 모바일 반응형
- [ ] 이미지 로드
- [ ] 에러 없음
```

---

## 10. 배포 후 관리

### 10-1. 업데이트 배포

```bash
# 1. 코드 수정

# 2. 로컬 테스트
npm run dev

# 3. 빌드 테스트
npm run build
npm run preview

# 4. Git 커밋
git add .
git commit -m "feat: 새로운 기능 추가"

# 5. GitHub 푸시
git push origin main

# 6. Vercel 자동 배포 (2-3분)
# 또는 수동 배포:
vercel --prod
```

### 10-2. 모니터링

```
Vercel 대시보드에서 확인:
- 배포 상태
- 트래픽 통계
- 에러 로그
- 성능 지표
```

---

## 11. 문제 해결 가이드

### 11-1. 자주 발생하는 문제

#### 문제: 빌드 실패
```bash
# 에러 확인
npm run build

# 가능한 원인:
# 1. TypeScript 에러
tsc --noEmit  # 타입 에러 확인

# 2. 의존성 문제
rm -rf node_modules package-lock.json
npm install

# 3. 캐시 문제
npm run build -- --force
```

#### 문제: 라우팅 안 됨
```
Hash 기반 라우팅 사용 확인:
- URL: http://example.com/#/channel (올바름)
- URL: http://example.com/channel (동작 안 함)
```

#### 문제: API 할당량 초과
```
YouTube Data API 할당량: 10,000 units/day

확인:
1. Google Cloud Console → API 대시보드
2. 할당량 및 시스템 제한

해결:
1. 할당량 증가 요청
2. 캐싱 구현
3. 요청 최소화
```

---

## 12. 고급 개발 팁

### 12-1. 성능 최적화

```typescript
// 1. React.memo로 불필요한 리렌더링 방지
export const VideoCard = React.memo(({ video }: Props) => {
  return <div>...</div>;
});

// 2. useMemo로 비싼 연산 캐싱
const sortedVideos = useMemo(() => {
  return videos.sort((a, b) => b.viewCount - a.viewCount);
}, [videos]);

// 3. useCallback으로 함수 재생성 방지
const handleClick = useCallback(() => {
  console.log('clicked');
}, []);

// 4. 이미지 lazy loading
<img loading="lazy" src={thumbnail} alt={title} />
```

### 12-2. 코드 분할

```typescript
// 동적 import로 코드 분할
const OpportunityFinder = lazy(() => import('../components/OpportunityFinder'));

<Suspense fallback={<Loading />}>
  <OpportunityFinder />
</Suspense>
```

### 12-3. 환경 변수 사용

```bash
# .env.local 파일 생성
VITE_API_BASE_URL=https://api.example.com
VITE_DEFAULT_RESULTS=50

# 코드에서 사용
const apiBase = import.meta.env.VITE_API_BASE_URL;
```

---

## 📞 추가 도움이 필요하면

### 관련 문서
- `/배포_완벽_가이드.md` - 배포 상세 가이드
- `/CURSOR_작업_가이드.md` - AI 개발 가이드
- `/README.md` - 프로젝트 개요
- `/USER_GUIDE.md` - 사용자 가이드

### 커뮤니티
- GitHub Issues
- Stack Overflow
- Discord/Slack

---

## ✅ 최종 체크리스트

```
개발 준비:
- [ ] Node.js, npm, Git 설치
- [ ] 코드 다운로드
- [ ] 의존성 설치

개발:
- [ ] YouTube API 키 발급
- [ ] 로컬 개발 서버 실행
- [ ] 모든 코어 기능 구현
- [ ] UI/UX 개선
- [ ] 테스트 완료

배포:
- [ ] 프로덕션 빌드 성공
- [ ] Vercel 배포 완료
- [ ] 배포된 사이트 확인
- [ ] 모든 기능 동작 확인

유지보수:
- [ ] Git 커밋 규칙 설정
- [ ] 업데이트 프로세스 확립
- [ ] 모니터링 설정
```

---

**🎉 축하합니다!**

이 가이드를 따라 완료하셨다면, 완전히 동작하는 YouTube 채널 분석 서비스를 개발하고 배포한 것입니다!

**다음 단계:**
- 사용자 피드백 수집
- 새로운 기능 추가
- 성능 최적화
- 확장 가능성 고려

**행운을 빕니다! 🚀**
